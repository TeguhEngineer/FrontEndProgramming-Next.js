/** @type {import('next').NextConfig} */
const nextConfig = {
    // isi konfigurasi kamu di sini, contoh:
    reactStrictMode: true,
    // basePath: '/some-path',
    eslint: {
        ignoreDuringBuilds: true,
    },
};

module.exports = nextConfig;
